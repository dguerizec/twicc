---
title: "Set agent defaults per project"
---

Tired of picking the same model and permission mode for every new session? In
a project's edit dialog, open the **Agent settings** tab and lock a default
provider, model, effort and more.

Every new session in that project starts with them — and worktrees inherit
their main repo's defaults. Use **Load from…** to copy a preset or another
project's settings.
