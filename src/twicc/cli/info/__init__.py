"""CLI subpackage implementing ``twicc info``.

Read-only inspection of providers, presets, commands, models, and
agent-settings choices. Pure read paths — no drop-request, no DB writes.
Every output is JSON on stdout for machine consumption.
"""
